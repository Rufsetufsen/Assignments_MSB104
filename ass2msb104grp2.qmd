---
title: "ass2msb104grp2"
format: html
editor: visual
---
```{r}
#| label: setup
#| include: FALSE
library (tidyverse)
library (PxWebApiData)
library(readxl)
library(purrr)
library(dineq)
library(psych)
library(flextable)
```

```{r}
# Merging datasett gini og tidyjoined 
GDP <- select(tidyjoined, NUTS2, Time, GDP_Capita)
GDP_per_capita_2017 <- filter(GDP, Time == 2017)
```

```{r}
by_day <- group_by(GDP_per_capita_2017, NUTS2, Time) 
# Summarize scores per name
summary_df <- GDP_per_capita_2017 %>%
  group_by(NUTS2) %>%
  summarise(
    total_score = sum(GDP_Capita),
    .groups = 'drop'  # optional, removes grouping after summarise
  )

```




